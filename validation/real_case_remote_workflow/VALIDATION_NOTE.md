# Validation Note: Real Case Remote Workflow Lane

## Purpose

This note records the first bounded real-case lane across:

- Garbage Collector
- Jigsaw
- Arbiter

The lane uses live Garbage Collector material from the local SQLite corpus rather than synthetic fixture-only input.

## Intended flow

`GC stored item -> Jigsaw artifact shaping -> Jigsaw kernel bundle -> current Arbiter membrane`

## Case

The bounded case uses a remote-workflow opportunity assembled from live Garbage Collector notes:

- primary item: remote workflow systems note
- supporting items: remote income workflow note, client offer pricing note, business ops invoice note

## Boundary rules

- no Arbiter redesign
- no kernel contract changes
- no validator changes
- Jigsaw uses a bounded local adapter over the live GC SQLite corpus for this proof

## Result

- selected GC items:
  - primary: `gc:item:45` (`Remote workflow systems Automation ideas for consulting projects.`)
  - supporting: `gc:item:8`, `gc:item:22`, `gc:item:14`
- bundle judgment: `aligned`
- bundle confidence: `0.72`
- Arbiter judgment: `promoted`
- Arbiter recommended action: `prioritise_for_review`

## What felt useful

- the lane starts from live Garbage Collector material rather than a pure synthetic fixture
- the primary item still passed through the Jigsaw artifact lane before the kernel lane
- Jigsaw produced an explicit kernel bundle rather than a direct score-only judgment
- the current Arbiter membrane was sufficient to turn the shaped case into a final bounded decision

## What remained artificial or manually shaped

- supporting evidence selection is still a bounded Jigsaw-side choice over known live GC items, not yet a generalized retrieval contract
- case shaping from GC material into `kernel_input/v1` is still heuristic and domain-bounded
- the lane uses deterministic Jigsaw kernels for this first real-case proof rather than the LM-backed bundle path

## Honest conclusion

This is the first bounded real-case proof across all three layers:

`GC stored material -> Jigsaw shaping -> Jigsaw kernel bundle -> Arbiter decision`

It is not yet a generalized production case-ingestion pipeline, but it does show that the current stack can take real corpus material from Garbage Collector and convert it into a promoted Arbiter-ready case without redesigning any major boundary.
