[project]
name = "jigsaw-poc"
version = "0.1.0"
description = "Minimal isolated Jigsaw kernel proof-of-concept"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.25.1",
    "pydantic>=2.11.7",
]
