# Kernel Contract Space

This directory is reserved for future kernel-family contract material inside Jigsaw.

It exists to make clear that the current artifact interoperability lane is only one lane inside the broader middle-layer architecture.

Current non-goal:

- do not implement kernel-family contract logic in this folder during the first artifact-lane proof

Current role:

- preserve explicit space for future `kernel.v1`-style or related middle-layer capability contracts without mixing them into the first slice

