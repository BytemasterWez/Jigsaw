# Kernel Capability Space

This directory is reserved for future kernel-family capability work inside Jigsaw.

It exists so the repo structure reflects the broader middle-layer role without forcing kernel-family runtime logic into the first artifact-lane proof.

Current non-goal:

- do not add kernel execution logic here during the first strict interoperability slice

